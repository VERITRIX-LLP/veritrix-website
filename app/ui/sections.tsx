import Link from "next/link";import{Arrow}from"./icons";
export function PageHero({eyebrow,title,children}:{eyebrow:string;title:string;children:React.ReactNode}){return <section className="page-hero"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><div className="lead">{children}</div></section>} export function SectionTitle({eyebrow,title,copy}:{eyebrow:string;title:string;copy?:string}){return <div className="section-title"><p className="eyebrow">{eyebrow}</p><h2>{title}</h2>{copy&&<p>{copy}</p>}</div>} export function Cta({title="Let’s make something useful.",copy="Tell us what you are working through. We’ll start with a thoughtful conversation."}:{title?:string;copy?:string}){return <section className="cta-band"><div><p className="eyebrow">Start here</p><h2>{title}</h2><p>{copy}</p></div><Link className="button button-light" href="/contact">Start a conversation <Arrow/></Link></section>} export function ReviewsEmptyState(){return <div className="empty-state"><span className="empty-number">—</span><div><h3>Client stories will appear here.</h3><p>We’ll only publish genuine, approved feedback. No placeholder endorsements.</p></div></div>}
